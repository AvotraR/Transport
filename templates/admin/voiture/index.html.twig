{% extends 'admin/_base.html.twig' %}

{% block title %}Voiture index{% endblock %}

{% block body %}
    <h1>Voiture index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>Place</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for voiture in voitures %}
            <tr>
                <td>{{ voiture.id }}</td>
                <td>{{ voiture.Numero }}</td>
                <td>
                        {%for place in voiture.place%} 
                            <tr>
                                {%for place in place%} 
                                    {%if place == false %}
                                        <td><input type="radio" name="" id="check" value="{{place}} readonly"></td>
                                    {%else%}
                                        <td><input type="radio" name="" id="" value="{{place}}" checked readonly></td>
                                    {%endif%}
                                {%endfor%}
                            </tr>
                        {%endfor%}
                    </form>
                </td>
                <td>
                    <a href="{{ path('app_voiture_show', {'id': voiture.id}) }}">show</a>
                    <a href="{{ path('app_voiture_edit', {'id': voiture.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_voiture_new') }}">Create new</a>
{% endblock %}
