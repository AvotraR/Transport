{% extends 'admin/_base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ voiture.Numero }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ voiture.place ? voiture.place|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voiture_index') }}">back to list</a>

    <a href="{{ path('app_voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('admin/voiture/_delete_form.html.twig') }}
{% endblock %}
